import React from 'react';
import blockMainPage from './blockMainPage.png';

const BlockImg = () => {
  return (
    <img
      src={blockMainPage}
      alt='Block Main Page'
      style={{ width: '100%' }}
    />
  );
};

export default BlockImg;
